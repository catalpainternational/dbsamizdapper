# Database connection for running tests
# Copy this file to .env and adjust if needed
# 
# The default assumes PostgreSQL is running on localhost:5435
# Start PostgreSQL with: docker compose up -d
# Or manually: docker run -d -p 5435:5432 -e POSTGRES_HOST_AUTH_METHOD=trust postgres:15

# Recommended: Use DB_PORT for easy port switching (useful for parallel branches)
DB_PORT=5435

# PostgreSQL version (for docker-compose, defaults to 15)
POSTGRES_VERSION=15

# Or use full connection string (alternative to DB_PORT)
# DB_URL=postgresql://postgres@localhost:5435/postgres

# Alternative: Use DBURL (also supported, for compatibility)
# DBURL=postgresql://postgres@localhost:5435/postgres
